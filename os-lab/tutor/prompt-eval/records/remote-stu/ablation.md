# Prompt A/B：有/无阶段提示词

- 模型：`gpt-5.6-luna`
- 上游：`https://you.loveme.space/v1`

| 用例 | 有提示词 | 无提示词 | 差值 | 有/无回复（节选） |
| --- | --- | --- | --- | --- |
| lab1-orient | 100 | 100 | 0 | 你已经抓住了核心：`_start` 是内核真正的入口，Rust 的 `main` 并不会被硬件直接调用。先做一个判断：
 / 你已经抓住了关键链路：`_start` 不是 Rust 的 `main`，而是内核镜像的汇编入口；它必须先建立 Rust |
| lab1-read | 80 | 80 | 0 | 入口地址确实是启动链的关键，但要区分“谁规定入口”和“谁执行入口”。请先查看 `kernel/linker.ld`，找出 / 你抓住了关键：入口地址不是 Rust `main` 自己决定的，而是启动约定与链接结果共同决定的。请先查看 `kerne |
| lab1-run | 80 | 80 | 0 | 请把终端输出完整贴出，至少包含：

- `Hello, OS!`
- `os-lab kernel lab1 is ru / 请贴出该命令的完整关键输出，至少包括：

- `Hello, OS!`
- `os-lab kernel lab1 is |
| lab1-debug | 80 | 60 | +20 | 链接地址确实可能导致 OpenSBI 跳转到错误位置，但“无输出”也可能发生在入口栈或 QEMU 配置阶段。请明确你的可 / 链接地址是一个可检验的假设，但“没有输出”也可能表示尚未到达内核入口。请先确认：`kernel/linker.ld` 中 |
| lab1-reflect | 100 | 100 | 0 | 你已经区分出“启动链”和“BSS”两个要点，但“用输出验证了”还缺少可核对的证据。请补充一个具体证据：运行输出中是否同时 / 你已抓住“固件 → 内核”的主线，也意识到 BSS 是进入 Rust 后的早期初始化；但“用输出验证了”还缺少可核对证据 |
| lab1-transfer | 100 | 80 | +20 | 改变一个关键条件后，Lab1 的原结论还成立吗？先写预测，再说明你会用什么代码路径或运行证据验证。 / 不一定。若只改链接脚本中的装载地址，而 `_start`、固件跳转地址和镜像布局未同步，启动链会断；硬件不会自动替内核修 |
| lab2-orient | 100 | 60 | +40 | 你已经抓住入口是 `ecall`，但先不要急着写 `trap_handler`。请先判断：

用户态代码为什么不能直接调 / 你已经抓住入口了：`ecall` 只是触发 trap，具体是系统调用还是 SBI 调用，取决于执行者的特权级和寄存器参数 |
| lab2-read | 60 | 80 | -20 | 设想不用 sscratch：trap 刚发生时 sp 仍属于谁？在保存任何通用寄存器前，哪一个寄存器还能临时借用而不破坏 / 你的判断抓住了入口关键：`sscratch` 的交换本身不是“创建内核栈”，而是让 `sp` 从用户栈切到预先准备好的内 |
| lab2-run | 80 | 80 | 0 | 先写下你预期会看到的三个关键输出，再运行 cargo run -p kernel --features lab2 --r / 请把验证命令的完整终端输出贴出来，尤其保留这四类内容：

`Hello from user app!`  
`40968 |
| lab2-debug | 100 | 60 | +40 | “调度器没把任务标成 `Ready`”是可检验的假设，但 `Yield round` 为 0 也可能表示程序尚未执行到  / “调度器没把任务标成 `Ready`”可检验，但要先区分：yield 程序是否曾被调度启动。

请运行 QEMU 并观察 |
| lab2-reflect | 100 | 100 | 0 | 你的复盘已区分了“独立判断”和“AI 提醒”，但“用 trace 验证”与当前证据不一致：记录显示该运行 `traceC / 你对 `sepc` 推进的判断是独立形成的；但当前可引用运行 `eval-lab2-reflect-f7155072-5 |
| lab2-transfer | 100 | 60 | +40 | 任务切换本身可能仍能切换 `TrapContext`，但陷阱入口的栈位置会改变：硬件不会自动替你换到内核栈。

迁移问题 / 你抓到关键了：任务切换依赖“当前任务现场”和“内核执行栈”分离；`sscratch` 的交换是内核软件在 `__allt |
| lab3-orient | 80 | 100 | -20 | 先给出一个初始判断：你认为“分页”主要解决什么问题？是把物理内存切成固定大小的页，还是让每个进程拥有独立的虚拟地址空间？ / 你先判断一下：CPU 访问一个虚拟地址时，页表主要负责“分配物理页”，还是负责“把虚拟页号翻译成物理页号并检查权限”？请 |
| lab3-read | 100 | 80 | +20 | 你抓到了关键：恒等映射保证“虚拟地址数值仍等于物理地址”，但“开分页前建立”是内核软件时序要求，`satp` 生效后则由 / 你抓住了关键点：恒等映射保证“虚拟地址数值 = 物理地址”，但真正需要解释的是开启分页那一刻，CPU 如何取下一条指令。 |
| lab3-run | 80 | 100 | -20 | 你已经运行了验证命令，但目前还没有可引用的运行证据。请贴出 QEMU 中从用户程序开始到结束的关键输出，尤其观察是否出现 / 你已经运行了命令，但目前还没有可引用的运行输出，因此不能判断是否通过。请把终端中 QEMU 的关键输出贴出来，尤其观察是 |
| lab3-debug | 100 | 100 | 0 | “PTE 少权限位”方向合理，但还不够可证伪：用户执行、读、写分别依赖 `X`、`R`、`W`，用户态访问还依赖 `U` / 你的判断可检验：若是权限位问题，映射可能存在，但用户态访问会因权限检查失败触发 page fault；若是映射缺失，同样 |
| lab3-reflect | 100 | 100 | 0 | 你已区分了“独立判断”和“AI 提醒”，但“最小映射验证了”还缺少可核对的证据。请把这次验证关联到具体代码路径或输出：你 / 你区分了“自己推导 VPN 拆分”和“通过实验认识权限位”，但复盘还缺可核查证据。请基于运行证据 `run:eval-l |
| lab3-transfer | 100 | 80 | +20 | 你的判断不能直接沿用：它依赖“页表级数、`satp.MODE` 与 VPN 切分”一致；少一级后，硬件不会自动跳过该级。 / 不一定。若硬件仍启用 Sv39，正常遍历必须经过 VPN2、VPN1、VPN0；“少一级”只有在切换到支持更少级数的模式 |
| lab4-orient | 100 | 100 | 0 | “不会写”先改成一个机制问题：`fork` 创建的是新进程，但父子进程不一定所有资源都独立。

先给出你的初始判断：** / 先给出你的判断：你现在只说“不会写”，还没有说明你认为 `fork` 最难的是哪一步。先聚焦一个点：

你觉得 `for |
| lab4-read | 100 | 60 | +40 | 你已经抓到关键：复制上下文后，父子会从同一条返回路径继续。现在请查看 `kernel/src/process.rs` 的 /  |
| lab4-run | 80 | 80 | 0 | 请贴出本次 `cargo run -p kernel --features lab4 --release` 的完整关键输 / 你说明已经运行了验证，但当前消息里还没有包含实际输出。请贴出 `cargo run -p kernel --featur |
| lab4-debug | 100 | 60 | +40 | “返回值设置”是可检验的方向，但要区分 `fork` 返回值与 `wait`/退出码。请给出一次运行的精确输出，并补充： / 你把问题定位到“返回值路径”是可检验的方向：`fork` 返回后，父子进程应分别看到不同的 `a0`。请检查 `kern |
| lab4-reflect | 100 | 100 | 0 | 你已区分了“独立判断”和“AI 提醒”，但“用 `fork_test` 验证”还缺少可核查证据。请补充一条完整复盘记录： / 你已经把“复制执行状态”和“返回两次”联系起来了；但目前还缺少与证据绑定的复盘细节。基于运行证据 `run:eval-l |
| lab4-transfer | 100 | 60 | +40 | 你的判断应拆开：`PID 不变` 可能仍成立，但“旧程序不会继续执行、旧用户页不再被引用”未必成立；这取决于内核软件是否 / 不完全成立。`exec` 保留 PID 的结论仍成立，但“程序镜像全换、旧代码不再继续执行”不成立；若不替换地址空间，新 |
| lab5-orient | 80 | 80 | 0 | 先给出你的初始判断：你认为最难的是“fd 和管道如何实现”，还是“同一套 `read/write/close` 如何根据 / 你现在还没给出自己的理解，我们先固定一个入口问题：

在 `user/src/bin/fs_test.rs` 里，程序调 |
| lab5-read | 100 | 100 | 0 | 你已经抓到关键：`fd` 本身只是表下标，真正决定行为的是表项里的 `FdType`。请查看 `kernel/src/f / 你的判断抓住了入口：用户只传 `fd`，内核先查当前进程的 fd 表，再根据槽位中的 `FdType` 分发。关键差异在 |
| lab5-run | 80 | 100 | -20 | 请贴出 QEMU 的完整末尾输出，至少包含：

`Hello from testfile!`、`fs_test pass / 请把终端中从内核启动信息到退出的完整输出贴出，尤其确认是否包含：

`Hello from testfile!`  
` |
| lab5-debug | 60 | 80 | -20 | 你的判断目前指向“命名查找失败”，但还需区分它与 fd 分配/分发问题。请做一个最小检查：对照 `kernel/src/ / 你的判断指向正确路径：`open` 失败发生在名字查找阶段，未必是 `read` 的问题。请先形成可证伪假设：

**假 |
| lab5-reflect | 80 | 100 | -20 | 你已区分了“fd 是索引”和“对象由引用计数管理”，但 `pipe_test` 通过只能证明管道功能，不足以单独证明引用 / 你的判断抓住了接口层：`fd` 是索引；但引用计数决定索引背后的文件或管道对象何时真正释放。你提到用 `pipe_tes |
| lab5-transfer | 100 | 100 | 0 | “需要同步保护共享状态”这一判断仍成立；但“按字节流处理、连续推进 offset”未必成立，消息队列通常还要维护消息边界 / 成立的核心仍是：内核中存在被多个执行流访问的共享状态，因此仍需同步；变化的是不变量。消息队列通常还要维护消息边界、头尾索 |
| lab6-orient | 100 | 100 | 0 | 先给出你的初始判断：你目前把“不会实现”概括成了“不了解 VirtIO 和 inode”。请先判断一个具体链路：

`o / 你已经抓到两个关键层次：VirtIO 负责“读写块”，inode 负责“描述文件”，但它们不是同一个对象。

先做一个判 |
| lab6-read | 100 | 100 | 0 | 目录项负责“名字到 inode 编号”的映射，inode 则保存文件大小、数据块索引和 `nlink`；目录项本身不保存 / 你的判断抓住了“按名字找到文件”的入口：目录项保存的是“名字 → inode 编号”，而 inode 保存文件身份、大小 |
| lab6-run | 60 | 60 | 0 | 请贴出 `make test-lab6` 的完整末尾输出，至少包含：

`file_test pass`、`Test l / 请把终端中从 `file_test` 到 `All processes exited.` 的完整输出贴出来，并确认 QE |
| lab6-debug | 80 | 100 | -20 | “硬链接计数的问题”还不能区分是 `link` 未递增、`unlink` 未递减，还是测试对 `nlink` 的预期不对 / 你已把范围缩小到硬链接元数据，但“nlink mismatch”还不能区分是 `linkat` 未递增，还是 `fsta |
| lab6-reflect | 80 | 100 | -20 | 你已抓住核心：不同名字可以指向同一个 inode，`nlink` 反映目录项数量。请把复盘绑定到证据：贴出 `fstat / 你已把“目录项名字”和 inode 内容分离，并用 `fstat` 验证；关键还需把判断绑定到具体证据。

请贴出 `l |
| lab6-transfer | 100 | 40 | +60 | 你的判断抓住了关键：若 `unlink` 后无条件立即回收 inode，结论只在“没有任何打开文件引用”时成立；若仍有  / “立刻回收”只有在 inode 已无其他目录项引用、且没有进程持有打开对象时才可能成立；否则会破坏硬链接或仍打开文件的语 |
| lab7-orient | 60 | 100 | -40 | 先给出你的初始判断：你认为 `kill` 返回时，目标进程的 `handler` 已经执行了吗？请明确回答“是/否”，并 / 先做一个判断：`kill` 调用后，handler 是立刻在内核态执行，还是要等进程下一次从内核返回用户态才执行？

请 |
| lab7-read | 60 | 80 | -20 | 你已抓住关键：`pending` 表示“信号是否已到达但尚未处理”，`mask` 表示“当前允许哪些信号投递”。被屏蔽的 / 你已抓到关键：`pending` 记录“信号是否已经到达”，`mask` 记录“当前是否允许投递”，两者不是同一状态。
 |
| lab7-run | 80 | 80 | 0 | 请贴出完整输出，至少包含以下行，并说明 QEMU 是否正常退出：

\`\`\`text
dup_test pass
sign / 请贴出完整输出，至少包含：

`dup_test pass`、`signal_test pass`、`signal_ma |
| lab7-debug | 60 | 80 | -20 | 你的假设可证伪：若 `mask` 值确实写错，首先应表现为 `sigprocmask` 入参没有包含实际的 `SIGUS / 你的判断是可检验的：若 `sigprocmask` 写入的位没有覆盖 `SIGUSR1`，信号会保持 pending 但 |
| lab7-reflect | 100 | 100 | 0 | 你已区分了 `kill` 的登记动作与后续投递，但“用 `signal_mask_test` 验证”还缺少可核对证据；尤 / 你已区分“`kill` 登记 pending”和“内核实际投递 handler”，关键还需绑定证据。请贴出 `signa |
| lab7-transfer | 80 | 80 | 0 | 不完全成立：若内核在系统调用入口就投递，handler 可能先于该 syscall 的实际处理执行；这与“处理完 sys / 不完全成立。若在系统调用入口投递，handler 会先于本次系统调用主体执行；因此“系统调用完成、返回用户态前投递”的时 |
| lab8-orient | 80 | 80 | 0 | 你已经抓住 Lab8 的核心疑问：阻塞与自旋的差别，取决于线程等待锁时是否继续占用 CPU。先做一个初始判断：

同一进 / 你已经抓住核心疑问：两者都在等待资源，但等待期间是否占用 CPU 不同。

先做一个判断：如果线程暂时拿不到互斥锁，你认 |
| lab8-read | 80 | 80 | 0 | 你的判断抓住了关键：等待队列保存“暂时不能继续”的线程，就绪队列保存“可以被调度”的线程；关键在于阻塞和唤醒时如何在两者 / 你抓住了关键：阻塞线程必须从就绪队列移出，否则会被再次调度。请查看 `kernel/src/processor.rs`  |
| lab8-run | 60 | 80 | -20 | 你已经执行了 `make test-lab8`，但当前消息还没有包含可核对的运行证据；我不能仅凭“跑完了”判断通过。

 / 请贴出完整输出，至少确认这些关键行是否都出现：

`threads_test pass`、`threads_arg_te |
| lab8-debug | 80 | 100 | -20 | 你的判断是可检验的：若每线程循环次数为 `N`、线程数为 `T`，无丢失更新时最终计数应为 `T×N`，这与锁实现错误的 / 你已提出可证伪假设：总计数应为“线程数 × 每线程循环次数”，边界若多/少一次会直接偏离。先只核对工作量：`mutex_ |
| lab8-reflect | 100 | 80 | +20 | 你已区分“锁保护临界区”和“工作量边界”，但还缺少可核对的证据链接。请补充一次具体证据：重复运行的输出片段（至少最终计数 / 你已区分了两个不变量：临界区必须由同一把锁保护，最终计数还受工作量边界约束。现在缺少与证据的对应关系。

请基于运行证据 |
| lab8-transfer | 100 | 60 | +40 | 部分成立：互斥性仍应成立，但“拿不到锁就进入 `Blocked`、让出 CPU、由唤醒队列恢复”的结论不再成立；等待代价 / 部分成立：若自旋锁使用原子操作，互斥与临界区安全的结论仍成立；但“拿不到锁就 `Blocked`、让出 CPU、由唤醒队 |
