[package]
name = "os-sync"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Blocking mutex, semaphore, and condvar for the OS teaching lab (lab8+)"

[dependencies]
spin = "0.9"

[lib]
