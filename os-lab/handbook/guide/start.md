# 认识 os-lab

欢迎来到 **os-lab**。如果你是第一次打开这套实验，请从本页开始。

课程教材是 **《操作系统导论》**（*Operating Systems: Three Easy Pieces*，简称 OSTEP）。书里用三块「简单」内容组织整门课：**虚拟化（Virtualization）、并发（Concurrency）、持久性（Persistence）**。只读懂书还不够——要把这些抽象变成一台真能跑起来的机器，需要亲手走完「从裸机到可用内核」这条路。os-lab 会按 Lab 顺序带你搭起属于自己的操作系统；走完全部实验后，你手里会有一份完整、能在 QEMU 上运行的内核。

下面先看清：os-lab 是什么、要完成哪些 Lab、它们和教材怎么对应，以及建议怎样学。

## 一、os-lab 是什么

os-lab 是一套面向操作系统课程的教学实验环境，技术栈是：

- **语言**：Rust
- **目标平台**：RISC-V 64（`riscv64gc-unknown-none-elf`）
- **运行方式**：在本机用 QEMU 模拟一台 RISC-V 机器，跑你自己的内核

《操作系统导论》里，程序之所以能「以为」自己独占 CPU、拥有一整片地址空间、还能读写文件，是因为操作系统提供了这些**抽象**。实验里你要做的，就是亲手把这些抽象逐步实现出来——而不是只在应用层调用它们。

os-lab 的核心设计是：**始终只有一个内核代码库**。通过 Cargo 的 feature（如 `lab1`、`lab2`…）按阶段打开功能，让内核像搭积木一样逐步「长出」能力。你不用在好多套互不相关的内核工程之间来回切换，更容易看清操作系统是怎样一层层长起来的。

## 二、你将完成什么

整条学习路径规划为 **8 个 Lab**，按顺序推进。前 5 个已有完整实验材料；后 3 个主题与文档稍后补充，先占位，避免打乱学习顺序。

下表把每个 Lab 与《操作系统导论》的主题大致对齐（书中章序与实验顺序不必一一相同，先抓住「对应哪一块」即可）：

| Lab | 主题 | 对应教材主题（OSTEP） | 你将学到什么（概要） | 状态 |
|-----|------|----------------------|----------------------|------|
| Lab1 | 裸机启动与最小内核 | 导论基础：在「抽象」出现之前，机器如何跑起来 | 没有操作系统时，代码如何上电、如何输出、如何正常关机 | 已开放 |
| Lab2 | 中断处理与多任务 | **虚拟化（CPU）**：受限直接执行、陷入、上下文切换、调度入门 | 用户程序如何通过系统调用「请求」内核；多个任务如何轮流使用 CPU | 已开放 |
| Lab3 | 内存管理与虚存 | **虚拟化（内存）**：地址空间、分页与地址翻译 | 页帧、页表、地址空间：程序如何安全地使用「自己的那片内存」 | 已开放 |
| Lab4 | 进程管理 | **虚拟化（CPU）**：进程抽象与进程 API | `fork` / `exec` / `wait`：进程如何创建、替换与等待 | 已开放 |
| Lab5 | 文件系统与并发 | **持久性** + **并发**：文件与 I/O；锁与进程间通信 | 文件描述符与简单文件抽象、管道，以及内核中的自旋锁 | 已开放 |
| Lab6 | （待补充） | （待补充） | — | 占位 |
| Lab7 | （待补充） | （待补充） | — | 占位 |
| Lab8 | （待补充） | （待补充） | — | 占位 |

用教材的三大主题概括当前已开放部分：

```text
基础铺垫     Lab1（启动）→ Lab2（陷入 / 系统调用）
虚拟化       Lab2 / Lab4（CPU）+ Lab3（内存）
并发         Lab5（锁、管道）
持久性       Lab5（文件）
```

依赖关系可以简单记成一条链：

```text
Lab1 → Lab2 → Lab3 → Lab4 → Lab5 → Lab6 → Lab7 → Lab8
```

**请按顺序学习**：后面的 Lab 默认你已经理解前一阶段的机制。跳着做容易「命令能跑、道理对不上」。

> 读实验时建议同时翻教材：先用书里的问题建立直觉，再在代码里找「机制（mechanism）」长什么样。书里强调的「策略（policy）」在后续 Lab 会逐渐出现。

## 三、各 Lab 内容导读

### Lab1：裸机启动与最小内核

教材开篇会讲：操作系统负责管理硬件，并给程序提供易于使用的抽象。Lab1 回答一个更靠前的问题——**在这些抽象还不存在时，第一段内核代码如何活下来**。你将看到固件如何装入内核、为什么需要 `#![no_std]` / `#![no_main]`、如何打出 `Hello, OS!` 并关机。

- 实验指导：[Lab1 裸机启动](/labs/lab1-bare-metal)
- 验证命令见 [验证命令速查](/guide/verify)

### Lab2：中断处理与多任务

对应教材里 **CPU 虚拟化** 的关键机制：程序不能为所欲为地碰硬件，要通过**系统调用**陷入内核；内核用**上下文切换**和简单调度，制造「好多程序都在跑」的幻觉。

- 实验指导：[Lab2 中断与多任务](/labs/lab2-trap-and-task)

### Lab3：内存管理与虚存

对应教材 **内存虚拟化**：让每个程序以为自己拥有完整、私有的**地址空间**。你将接触物理页、页表与地址翻译，把用户程序装进各自的地址空间。

- 实验指导：[Lab3 内存与虚存](/labs/lab3-memory)

### Lab4：进程管理

对应教材中的**进程**抽象与 **进程 API**（尤其是 `fork` / `exec` / `wait`）。在 Lab2 任务机制之上，把「能切换的执行流」升级成更接近书中描述的进程。

- 实验指导：[Lab4 进程管理](/labs/lab4-process)

### Lab5：文件系统与并发

一头接上教材的**持久性**（文件、设备与数据如何长期保存/访问），一头接上**并发**（共享数据需要锁；进程间可用管道通信）。本实验用精简实现帮你把书里的概念落到内核代码里。

- 实验指导：[Lab5 文件系统与并发](/labs/lab5-fs-and-sync)

### Lab6–Lab8：（待补充）

> 这三个 Lab 的主题、与教材章节的对应关系，将在后续版本写入本手册。请先完成 Lab1–Lab5；开放后会更新本页表格。

- Lab6：待补充
- Lab7：待补充
- Lab8：待补充

更细的知识地图与 feature 说明，也可对照 [实验总览](/labs/overview)（其中仍以当前已实现的 Lab1–5 为主）。

## 四、建议的学习方式

把每个已开放的 Lab 当成一个小闭环，按下面节奏做，通常最省事：

1. **先对一下教材**  
   看本页表格里该 Lab 对应 OSTEP 的哪一块（虚拟化 / 并发 / 持久性），用书里的问题建立动机，再打开实验文档。

2. **读问题场景，对照代码**  
   文档里的图（启动流程、trap、页表等）建议对着仓库源文件看一遍。书里的「抽象」在代码里往往对应一组具体机制。

3. **跑通验证**  
   在仓库根目录激活环境后进入 `os-lab`，用对应 feature 跑内核（详见下方「第一次动手」与 [验证命令](/guide/verify)）。看到文档写的期望输出，再继续。

4. **完成【任务二】阅读理解与思考题 / 小修改**  
   能讲清楚「为什么这样写」，比只看到 `pass` 更重要。题目在各 Lab 实验文档【任务二】，答案在 [参考答案](/answers/README)。

5. **勾选进度**  
   在 [学习进度](/guide/progress) 页勾选完成项，方便下次接着做。

6. **善用 AI，但先自己想**  
   每个 Lab 文档末尾有「AI 提问模板」。提问时可以带上教材术语（如地址空间、系统调用、锁），效果通常更好。

遇到卡住时，优先检查三件事：环境是否激活、feature 是否选对、是否在 `os-lab` 目录下执行命令。

## 五、第一次动手（最短路径）

若尚未安装 Rust / QEMU，先看 [环境安装](/setup/environment)。

在仓库**根目录**打开 PowerShell：

```powershell
. .\scripts\activate-os-env.ps1
cd os-lab
```

自检：

<CopyCommand command="rustc --version
qemu-system-riscv64 --version" />

跑通 Lab1：

<CopyCommand command="cargo run -p kernel --features lab1 --release" />

成功时应在 OpenSBI 日志之后看到类似：

```text
Hello, OS!
os-lab kernel lab1 is running on QEMU virt.
```

然后按顺序体验后续已开放的 Lab：

<CopyCommand command="cargo run -p kernel --features lab2 --release
cargo run -p kernel --features lab3 --release
cargo run -p kernel --features lab4 --release
cargo run -p kernel --features lab5 --release" label="复制全部" />

> Lab6–Lab8 的运行方式将随实验材料一并补充。

## 六、推荐阅读顺序

| 顺序 | 做什么 | 链接 |
|------|--------|------|
| 1 | 读完本文，建立「实验 ↔《操作系统导论》」图景 | 本页 |
| 2 | 配好本机环境 | [环境安装](/setup/environment) |
| 3 | 看知识地图与仓库结构（可选） | [实验总览](/labs/overview) |
| 4 | 从 Lab1 正式开始（建议同步翻教材导论部分） | [Lab1](/labs/lab1-bare-metal) |
| 5 | 每完成一阶段，回来勾进度、查验证命令 | [学习进度](/guide/progress) · [验证速查](/guide/verify) |

准备好了的话，下一步直接进入 [Lab1：裸机启动与最小内核](/labs/lab1-bare-metal)。
