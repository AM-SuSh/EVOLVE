schema_version: 1
lab_id: lab6
checkpoints:
  - { id: C6-1, stage: orient, required_evidence: [manual], prompt: 说明 VirtIO、块、inode、目录项和 fd 的层次。 }
  - { id: C6-2, stage: read, required_evidence: [code], prompt: 找到 DiskFs::link 更新 aliases 与 nlink 的边界。 }
  - { id: C6-3, stage: run, required_evidence: [run], prompt: 保存带 VirtIO 的八条可信断言。 }
  - { id: C6-4, stage: debug, required_evidence: [run, code], prompt: 用现象判断失败是内核漏 bump nlink，还是用户测例断言写错。 }
  - { id: C6-5, stage: reflect, required_evidence: [report], prompt: 解释为何 file_test 仍过、link_test 失败，能帮助锁定 DiskFs::link。 }
