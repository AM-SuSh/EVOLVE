# os-lab 实验技术文档（汇总版）

> 版本：2026-08-11
> 初赛基线：2026-06-30 的 `61fadb8` 提交
> 说明：本文档由 `new-docs/` 下的分方向小文档合并而成，覆盖本次相对初赛新增、重构与优化的完整技术内容。

---

## 1. 总览与基线对比

### 1.1 一句话总结

初赛交付的是“参考练习全绿 + 自研 Lab1-5 教学内核 + 静态学习手册”；
本次新增的是“Lab6-8 完整内核链路 + 账号制学习工作台 + 教师端 + AI 导师 + AI 学习评价 + 知识库 RAG + Lab Factory + 可信证据链 + 部署运维闭环”。

项目定位也从“一套教学实验环境”扩展为“一套可进课堂的操作系统教学与评价系统”。

### 1.2 初赛基线与本次状态对比

| 维度 | 初赛基线 | 本次状态 |
| --- | --- | --- |
| 参考练习 | 5 章 exercise 全部通过 | 保持全绿，证据与补丁继续保留 |
| 内核 Lab | Lab1-5 | Lab1-8 |
| 组件 crate | 6 个 | 8 个，新增 os-signal、os-sync |
| feature gate | lab1-lab5 | lab1-lab8 |
| 文件系统 | 内嵌只读文件 | Lab6 升级为 VirtIO + easy-fs 磁盘 FS |
| 进程/并发 | fork/exec/wait、自旋锁、管道 | 新增信号、统一 fd、线程、阻塞同步、死锁检测 |
| 学习手册 | VitePress 静态站点 | 账号制学习工作台，含代码、终端、Problems、测试结果、Trace、报告 |
| 教师端 | 无 | 班级、开放进度、任务变体、AI 配置、报告查看 |
| AI 使用 | 开发期 AI 协作记录 | 平台内置 AI 导师 + AI 学习评价双 Agent |
| AI 导师 | 无平台化实现 | 七类问题意图、L0-L4 提示、答案护栏、证据门控、RAG |
| 知识库 | 无 | 多来源知识库、FTS + 向量混合检索、教师审核发布 |
| 评分 | 无 | Rubric v3、14 细项、掌握画像、教师复核 |
| Lab 内容生产 | 手工 Markdown | Lab package、变体、lint/dry-run/test/publish |
| CI/测试 | 手工验证 | GitHub Actions、npm test、smoke、day7 回归 |
| 部署运维 | 本地环境脚本 | Tutor Server、TLS 建议、SQLite 备份恢复、数据最小化 |
| 文档体系 | 技术方案 + 设计/对比/AI 记录 | 新增平台、导师、评分、部署、教师指南等完整体系 |

### 1.3 没有单独成文的内容

以下内容在初赛后没有发生实质变化：

- 参考环境 ch3/ch4/ch5/ch6/ch8 的 base 与 exercise checker 结果保持全绿。
- 基础环境安装与激活脚本的主体流程沿用。
- 初赛的“参考练习 + 自研环境”双线框架本身保持不变。
- 初赛已交付的许可证、仓库结构和技术方案基调继续沿用。

### 1.4 新增文件变更分布

相对初赛基线 `61fadb8`，主要新增/修改目录分布：

| 目录 | 变更文件数 | 说明 |
| --- | ---: | --- |
| os-lab/tutor | 176 | AI 导师、状态机、契约、Harness、RAG、Prompt |
| os-lab/handbook | 83 | 工作台、教师端、导师服务、测试、手册 |
| os-lab/lab-packages | 69 | Lab package、变体、releases |
| os-lab/learning | 57 | 评分、复核、知识库、试用、数据库 |
| os-lab/labs | 25 | Lab6-8 文档、答案与 Lab1-5 重构 |
| os-lab/user | 24 | 新增用户态测试程序 |
| os-lab/kernel | 20 | Lab6-8 内核模块与重构 |
| os-lab/scaffold | 14 | fill/debug 变体 |
| os-lab/docs | 10 | 二期文档 |
| os-lab/os-sync | 7 | 新组件 crate |
| os-lab/os-signal | 3 | 新组件 crate |

说明：

- 该统计来自 `git diff --name-status 61fadb8 HEAD`。
- 总变更 517 个文件，包含新增 PDF、删除旧演示视频、生成文件与大量文档。
- 实际“功能新增”核心集中在 os-lab 下的 tutor、handbook、learning、lab-packages、kernel、user。

### 1.5 初赛已有但本次被重构的内容

| 初赛内容 | 本次变化 |
| --- | --- |
| `kernel/src/fs.rs` | 拆分为 `fs/embedded.rs` + `fs/disk.rs` |
| `os-fs` 组件 | 从内嵌只读表扩展为磁盘 FS 支撑 |
| 静态 VitePress 手册 | 扩展为登录制学习工作台 |
| `LabProgress.vue` | 被 `LabWorkspace`、`JourneyRail` 等取代 |
| `guide/progress.md`、`quick-start.md`、`verify.md` | 整合为 `start.md`、`beginner.md` |
| Lab1-5 独立 exercise 目录 | 思考题并入正文【任务二】 |
| AI 协作记录 | 升级为平台内 AI 导师 + AI 评价双 Agent |
| 本地手工验证 | 增加 CI、Harness、smoke、day7 回归 |
| 单机环境脚本 | 增加 Tutor Server、SQLite、备份恢复、部署文档 |

---

## 2. 内核与实验内容

> 初赛基线：Lab1-5、6 个组件 crate、内嵌只读文件系统。
> 本次新增：Lab6-8、os-signal、os-sync、磁盘 FS、信号、线程同步、死锁检测。

### 2.1 新增 Lab6：磁盘文件系统

新增模块：

- `kernel/src/virtio_block.rs`：VirtIO 块设备
- `kernel/src/fs/disk.rs`：磁盘文件系统
- `kernel/src/fs/embedded.rs`：原 `fs.rs` 迁移后的内嵌只读文件系统
- easy-fs 磁盘镜像与 `fs.img`
- `mmap` / `munmap`、`spawn`、stride 调度、`set_priority` 作为前置能力

新增 syscall：

- `mmap` 222
- `munmap` 215
- `spawn` 400
- `set_priority` 140
- `linkat` 37
- `unlinkat` 35
- `fstat` 80

新增验证：

```text
make test-lab6
```

关键输出：

```text
file_test pass
Test link OK!
mass open/unlink OK!
mmap_test pass
spawn_test pass
stride_test pass
fs_test pass
pipe_test pass
```

### 2.2 新增 Lab7：IPC 与信号

新增内容：

- 统一 fd 抽象：Regular、PipeRead、PipeWrite
- `dup` 复制 fd 表项
- 信号 syscall：`kill`、`sigaction`、`sigprocmask`、`sigreturn`
- 新组件 crate：`os-signal`
- 信号默认行为、屏蔽、投递时机
- 管道 fd 在 fork 后引用计数继承

新增验证：

```text
make test-lab7
```

关键输出：

```text
dup_test pass
signal_test pass
signal_mask_test pass
pipe_test pass
```

### 2.3 新增 Lab8：线程与同步

新增模块：

- `kernel/src/processor.rs`：进程内多线程
- `kernel/src/sync_syscall.rs`：阻塞式同步原语 syscall，也是 Lab8 fill/debug 动手点
- `kernel/src/deadlock.rs`：死锁检测
- `os-sync`：新组件 crate

新增 syscall：

- 线程：`thread_create` 1000、`gettid` 1001、`waittid` 1002
- mutex：create/lock/unlock 1010-1012
- semaphore：create/up/down 1020-1022
- condvar：create/signal/wait 1030-1032
- 死锁：`enable_deadlock_detect` 469

新增验证：

```text
make test-lab8
```

关键输出：

```text
threads_test pass
threads_arg_test pass
mutex_test pass
condvar_test pass
pipetest passed!
deadlock test mutex 1 OK!
deadlock test semaphore 1 OK!
pipe_test pass
```

### 2.4 新增用户态测试程序

Lab6：

- `file_test`：磁盘文件 open/read/close 读写测试
- `link_test`：link 硬链接与 fstat 状态测试
- `mass_unlink_test`：批量 open/unlink 与路径清理测试
- `mmap_test`：mmap/munmap 映射与权限测试
- `spawn_test`：spawn 从 fs.img 加载并运行新程序测试
- `stride_test`：stride 调度与优先级测试
- `lab6_usertest`：Lab6 汇总用户测试入口

Lab7：

- `dup_test`：dup fd 复制与共享测试
- `signal_test`：kill/sigaction 信号投递与处理测试
- `signal_mask_test`：sigprocmask 信号屏蔽测试
- `signal_child`：配合信号测试的子进程
- `lab7_usertest`：Lab7 汇总用户测试入口

Lab8：

- `threads_test`：线程创建、等待与退出码测试
- `threads_arg_test`：线程参数传递测试
- `mutex_test`：阻塞 mutex 临界区计数测试
- `condvar_test`：条件变量 wait/signal 测试
- `pipetest`：fork + 管道通信测试
- `deadlock_mutex_test`：重复加锁触发 `-0xDEAD` 测试
- `deadlock_sem_test`：信号量环路死锁测试
- `lab8_integration_test`：Lab8 全链集成测试，默认 initproc
- `lab8_usertest`：Lab8 汇总用户测试入口

### 2.5 代码规模变化

- 源码文件：约 70 个 Rust/汇编文件
- 代码行数：约 8353 行
- workspace crate：11 个，其中组件 crate 8 个
- 依赖层数：2 层
- host 单元测试：24 项扩到 40+ 项

详细文档：

- `os-lab/docs/architecture.md`：workspace、feature gate、数据流架构说明
- `docs/lab6-8.md`：Lab6-8 二期总览、依赖、syscall 接口与验收
- `os-lab/labs/lab6-disk-fs.md`：Lab6 磁盘文件系统学生手册
- `os-lab/labs/lab7-ipc-signal.md`：Lab7 IPC 与信号学生手册
- `os-lab/labs/lab8-thread-sync.md`：Lab8 线程与同步学生手册

---

## 3. 学习平台与学生工作台

> 初赛基线：VitePress 静态学习手册，只有进度勾选、快速开始、验证命令。
> 本次新增：账号制学习平台、代码工作台、可信验证、Trace、实验报告、学习数据持久化。

### 3.1 账号与身份

- 注册/登录，注册必填班级
- 学生账号绑定代码工作区、学习事件、报告、AI 会话
- 游客模式移除，站点入口即登录
- 学生代码工作区统一在 `student-labs/<用户名>/`

### 3.2 工作台

新增区域：

- 左侧：实验手册
- 右侧上方：Monaco 代码编辑器
- 右侧下方：终端、Problems、测试结果
- 学习支持区：Trace、实验报告、学习评价
- 右下角：AI 导师

新增前端组件：

- `AuthGate`：登录、注册、班级选择与权限门
- `LabWorkspace`：Lab 工作台主布局
- `ManualPane`：左侧实验手册阅读
- `CodePanel`：代码编辑、文件状态与保存
- `TerminalPanel`：终端运行与输出
- `ProblemsPanel`：编译诊断展示与源码跳转
- `TraceViewer`：Trace 查看与回放
- `ReportPanel`：实验报告编辑、附件与提交
- `AssessmentPane`：学习评价细项与证据展示
- `TutorPane`：AI 导师对话入口与面板
- `TeacherNav`：教师端导航
- `TeacherPublishPanel`：教师发布、开放进度与任务分配
- `TeacherReport`：学生报告查看与反馈
- `TeacherReview`：教师复核队列
- `KnowledgeManager`：知识库管理
- `HomeLanding`：首页落地页
- `MaterialsShelf`：学习材料架

### 3.3 可信验证

- 服务端可信 recipe
- runId + workspaceVersion
- 命名断言
- 结构化 Trace
- Problems 编译诊断可跳回源码
- 无证据时显示空态，不展示 mock 数据

关键约束：

- 自定义命令退出码 0 不产生 verified=true
- Trace 绑定 runId，跨账号访问返回 404
- 查看 Trace 会记录 `trace_inspected` 学习事件

### 3.4 实验报告

- Markdown 编辑与预览
- 模板与自由编辑
- 图片附件上传、引用、自动清理
- 草稿保存、断线重试、本机地址回退
- 正式提交生成 `reports/<labId>/submission.md`
- 报告可插入 run/trace 证据引用

### 3.5 学习数据持久化

- SQLite：`learning/os-lab.db` 保存账号、权限、运行状态、断言、评分、报告索引
- 文件系统：`student-data/<userId>/` 保存报告、会话、事件、运行制品
- 服务端保存会话与事件 JSONL
- 解锁规则：可信成功运行 + 带反思报告，再结合教师开放进度

详细文档：

- `os-lab/handbook/guide/start.md`：入门指南，Lab1-8 与 OSTEP 映射、学习方式与最短路径
- `os-lab/handbook/guide/beginner.md`：新手第一次完成 Lab 的完整操作流程
- `os-lab/handbook/README.md`：手册站点结构、内容同步与构建方式
- `os-lab/handbook/docs/workbench-ui.md`：工作台 UI 组件与交互说明

---

## 4. 教师端与教学管理

> 初赛基线：无教师端。
> 本次新增：教师控制台、开放进度、任务变体、AI 统一配置、班级与报告管理。

### 4.1 教师控制台

新增 `/teacher` 教师端：

- 全局、班级、学生三级发布对象
- 作业公告
- 教学进度开放
- 任务分配
- 个别调整
- AI 统一模型配置
- 学生进度列表
- 学生报告在线查看
- 班级名单与导出
- 一键重置学生工作区

### 4.2 任务变体

新增 fill/debug 变体：

| Lab | 变体文件 | 变体内容 |
| --- | --- | --- |
| Lab2 | `kernel/src/task.rs` | 协作式调度与任务切换的补全/排错 |
| Lab3 | `kernel/src/mm.rs` | 页表映射与权限管理的补全/排错 |
| Lab4 | `kernel/src/process.rs` | fork/exec/wait 进程语义的补全/排错 |
| Lab5 | `kernel/src/fs/embedded.rs` | fd 表与文件读写的补全/排错 |
| Lab6 | `kernel/src/fs/disk.rs` | 磁盘 FS、link/fstat 的补全/排错 |
| Lab7 | `kernel/src/signal.rs` | 信号注册、投递与屏蔽的补全/排错 |
| Lab8 | `kernel/src/sync_syscall.rs` | 阻塞式同步 syscall 的补全/排错，如 `finish_blocking_syscall`、`sys_mutex_unlock` |

变体统一放在 `os-lab/scaffold/exercises/`，教师可下发 fill、debug 或 random 随机变体。

### 4.3 AI 统一配置

- 教师填写 OpenAI-compatible 接口地址、模型名、API Key
- 可强制全班统一，或允许学生自配
- 教师端只显示“已设置”，不回填真实 Key
- 支持显式清除统一配置
- 可检测 `/models` 连接状态

### 4.4 账号与安全

- 预置管理员 `admin/admin123`，首登必须改密码
- 教学配置保存在 `os-lab/scaffold/teacher.json`
- 学生账号无法访问教师端
- 支持班级管理和班级筛选

详细文档：

- `教师使用指南.md`：教师端部署、开课流程、任务分配、AI 配置、进度与报告查看
- `os-lab/handbook/guide/ai-tutor.md`：教师/学生引导式学习入口页面说明

---

## 5. AI 导师

> 初赛基线：只在文档中记录“如何与 AI 协作”，无平台内 AI 导师。
> 本次新增：服务端 AI 导师、七类问题意图、提示等级、答案护栏、证据门控、RAG、评测 Harness。

### 5.1 服务端权威控制

核心文件：

- `os-lab/tutor/turn-policy.mjs`：本轮意图识别、问题线程与教学动作规划
- `os-lab/tutor/state-machine.mjs`：旧阶段状态机，用于兼容与遥测
- `os-lab/tutor/evidence-refs.mjs`：run/trace/diag/event 证据引用校验
- `os-lab/tutor/trace-store.mjs`：Trace 存储、哈希校验与分页查询
- `os-lab/handbook/tutor-server.mjs`：导师服务端，负责 Prompt 组装、模型调用与输出校验

核心流程：

```text
学生消息 + Lab + 证据
-> 身份/Lab/证据校验
-> 意图识别与问题线程
-> 答案护栏
-> 可信证据与 RAG 上下文
-> 模型调用
-> 输出校验
-> 返回回复、意图、动作、合法引用
```

### 5.2 七类问题意图

| 意图 | 适用问题 |
| --- | --- |
| concept | 概念与机制 |
| code-reading | 源码与调用链 |
| debug | 报错与失败现象 |
| verification | 运行与测试 |
| reflection | 复盘与理解变化 |
| transfer | 对比与迁移 |
| direct-answer | 直接要答案 |

### 5.3 提示等级

- L0：观察现象与已有材料
- L1：形成假设
- L2：设计最小验证
- L3：对照路径
- L4：停止泄漏实现

提示等级按 `topicKey` 问题线程累计，换题、换文件、换诊断后归零。

### 5.4 答案护栏与证据门控

- 不输出完整文件、完整 patch、可直接提交实现
- 不伪造服务端 run/trace/diag 证据
- 引用只能来自本轮白名单
- 越权引用会丢弃原回复并返回安全追问
- 没有可信 run 时不得声称验证通过

### 5.5 Prompt 体系

- `tutor/prompts/system.md`：全局导师人设、教学边界与回答风格
- `tutor/prompts/<lab>/context.md`：Lab 专属机制、允许讨论范围与验证标准
- `tutor/prompts/strategies/*.md`：七类问题意图的回答策略
- `tutor/prompts/guardrails.yaml`：完整答案、参考实现与越权请求硬规则

### 5.6 评测结果

- Tutor Harness：34/34
- RAG Harness：3/3
- V3 离线全链路 19 条：综合 94
- 真实模型一轮：19 条全部一次成功，综合 96
- 跨阶段一致率：100%

详细文档：

- `os-lab/docs/ai-tutor-stage-guide.md`：AI 导师意图、提示等级、Prompt 组合与迁移兼容
- `os-lab/docs/agent-system-technical.md`：AI Tutor/Assessment/知识库 RAG 的技术契约
- `os-lab/docs/member-c-c0-c7-guide.md`：成员 C 在 AI 导师方向的 C0-C7 完成情况与接口

---

## 6. AI 学习评价与教师复核

> 初赛基线：无学习评分体系。
> 本次新增：Rubric v3、14 细项、掌握画像、教师复核门控、评分审计与校准。

### 6.1 Rubric v3

评分结构：

```text
total = 0.45 x process + 0.35 x result + 0.20 x reflection
```

14 个细项：

- process：问题推进、学生判断、证据引用、可证伪假设、可信验证、失败后迭代
- result：Hello、Power、Yield 五轮、全部退出等命名断言
- reflection：独立判断、AI 与验证分工、迁移对照、退出码反例意识

无证据项必须显示“未观察到”，不能凭模型建议补成确定分数。

### 6.2 掌握画像

- `GET /mastery` 返回概念掌握状态
- 按概念聚合误区、提示等级、置信度、评价引用
- 评价结果反馈给导师，用于后续个性化追问

### 6.3 教师复核门控

强制复核：

| 代号 | 触发 |
| --- | --- |
| H1 | 结果分满分但过程分极低 |
| H2 | Yield 断言存疑 |
| H3 | 反思满分但无 run 证据引用 |
| H4 | 多次护栏触发仍高过程分 |
| H5 | 变体自动分与教师抽检不一致 |
| H6 | 学生申诉 |

建议复核：

- S1：迁移题与本 Lab 证据矛盾
- S2：长时间停滞后突然满分
- S3：同班高度相似报告

### 6.4 评分审计

- 教师改分保留原自动分
- 教师修正、理由、证据引用、revision 全部留存
- AI 不拥有最终成绩裁决权
- 评分细项可点击回看 run/trace/报告

### 6.5 校准

- 使用 20 条人工标注轨迹离线校准
- 建议就绪阈值从 70 调整为 76
- 假阳性从 3 降为 0
- 准确率从 0.80 提升到 0.95
- 明确标注：构造/标注样本，不是真人试用数据

详细文档：

- `os-lab/learning/rubric-v3.mjs`：Rubric v3 评分规则与证据引用实现
- `os-lab/learning/teacher-review-gates.md`：H1-H6 强制复核与建议复核门控
- `os-lab/learning/ai-interaction-scoring.md`：AI 交互过程评分方案详解
- `os-lab/learning/trial-revision-record.md`：20 条标注轨迹校准与修订记录

---

## 7. 知识库与 RAG

> 初赛基线：无知识库与检索。
> 本次新增：多来源知识库、多格式规范化、章节分块、FTS + 向量混合检索、教师知识工作台。

### 7.1 知识来源

- 平台 Lab 手册：Lab 任务、平台行为与验证标准的最高权威
- Lab package concept 与检查点：机制概念、检查点与迁移题
- 发布目录：系统版本元数据，不参与自由文本 RAG
- OSTEP 中文核心章节：虚拟化、并发、持久化教材内容
- RISC-V Reader 中文版：RISC-V ISA 与特权架构背景
- LearningOS 讲义：课程讲义正文
- rCore Tutorial Guide：内核实现参考
- CSAPP 中文电子书：内存、进程、链接与并发补充

文档记录规模：

- 8 个 Source
- 约 1964 个 active Chunk
- 约 1940 条同时进入 FTS 与向量缓存
- 后续构建基线：1384 active Chunk、1360 条向量缓存
- 实际数量以当前 SQLite 统计为准

### 7.2 多格式规范化

支持：

- Markdown
- HTML
- JSON/YAML
- TXT
- RST
- PDF
- EPUB
- DOCX

统一输出 `Document` 模型，保留标题层级、章节路径、代码语言、HTML anchor、PDF page/line locator。

### 7.3 章节感知分块

- 相邻 block 只有 `sectionPath` 相同才合并
- 每个 chunk 保留 block ordinals、locator、父标题链
- 默认目标长度 1000 字符，硬上限 1400 字符
- 不做隐式 overlap

### 7.4 SQLite 与检索

- 独立知识库：`learning/knowledge/knowledge.db`
- FTS5 trigram 支持中文子串检索
- 一两字查询使用 LIKE 回退
- FTS + embedding + Reciprocal Rank Fusion
- 权限、版本、active/indexable、Lab/global 上限硬截断
- Tutor 最多 5 个 chunk，公共层最多 2 个
- 直接索要答案时跳过知识检索

### 7.5 教师知识工作台

新增 `/teacher/knowledge`：

- 左栏：Global/Lab1-8 与来源状态
- 中栏：版本、章节路径、Chunk 摘要
- 右栏：正文、自动归属依据、审核表单、版本/Chunk 操作
- 支持 PDF/EPUB/MD/TXT/DOCX 上传
- 默认 teacher-only，审核后才进入发布索引
- 发布、停用、回滚全部写审计日志

详细文档：

- `os-lab/docs/agent-system-technical.md`：知识库分层、检索与 Tutor RAG 技术契约
- `os-lab/learning/knowledge/README.md`：知识源登记、去重、构建与发布规则

---

## 8. Lab Factory 与课程内容生产

> 初赛基线：实验内容以手工 Markdown 为主。
> 本次新增：Lab package、冻结契约、fill/debug 变体、lint/dry-run/test/publish 发布流水线。

### 8.1 Lab Package

新增 `os-lab/lab-packages/`：

- Lab2：样板包
- Lab3：第二样板
- Lab4-8：lab.yaml、concepts、checkpoints、fill/debug

每个 Lab package 包含：

- Lab 元数据
- 知识点索引
- 文件清单
- 验证命令
- 任务变体
- 检查点与迁移题

### 8.2 冻结契约

- Lab spec v1：Lab 教学规格
- event v2：学习事件 schema
- run-result v1：可信运行结果
- trace v1：教学 Trace

权威源：

- `tutor/schema/lab-spec-v1.schema.json`：Lab 规格校验 schema
- `tutor/schema/event-v2.schema.json`：学习事件校验 schema
- `tutor/schema/run-result-v1.schema.json`：可信运行结果校验 schema
- `tutor/schema/trace-v1.schema.json`：教学 Trace 校验 schema
- `tutor/schema/m0-contract-baseline-v1.json`：M0 四契约版本清单与兼容规则

### 8.3 Lab Factory 流水线

```text
lint -> dry-run -> test -> publish
```

CLI 示例：

```bash
npm run lab-factory -- lint lab3
npm run lab-factory -- dry-run lab3 --variant debug
npm run lab-factory -- test lab3 --variant debug --author teacher
npm run lab-factory -- publish lab3 --test-run-id <id> --teacher teacher --approval-note "测试通过，批准发布"
```

发布生成不可变 release 目录：

```text
os-lab/lab-packages/releases/lab2/1.0.0/
os-lab/lab-packages/releases/lab3/1.0.0/
...
os-lab/lab-packages/releases/lab8/1.0.0/
```

详细文档：

- `os-lab/lab-packages/README.md`：Lab package 结构、样板与协作约定
- `os-lab/docs/lab2-m0-acceptance.md`：Lab2 纵向闭环验收与契约冻结
- `os-lab/docs/day7-demo-runbook.md`：Day7 全链路演示与回归

---

## 9. 工程质量、测试与 CI

> 初赛基线：本地手工验证为主。
> 本次新增：GitHub Actions、npm test、smoke、Harness、Day7 回归、更完整的 QEMU 与静态检查。

### 9.1 CI

新增 `.github/workflows/os-lab-ci.yml`，本地与 CI 共用：

```bash
npm run test:day7
```

覆盖：

- npm test
- Tutor Harness
- RAG Harness
- Tutor smoke
- VitePress production build
- 链接检查

### 9.2 测试规模

| 测试 | 结果 |
| --- | --- |
| 组件 host 单测 | 40+ 项 |
| JS/契约测试 | 97 项，全部通过 |
| Tutor Harness | 34/34 |
| RAG Harness | 3/3 |
| Tutor smoke | 通过 |
| Day7 全链路 | 一次通过，约 88 秒 |

2026-08-10 已修复此前唯一的 Lab7 debug source assertion 失败：

- `lab-factory.test.mjs` 的断言从已迁移的用户态 starter 文件改为实际变体文件 `kernel/src/signal.rs`
- 修复后 `npm test` 为 97/97

### 9.3 QEMU 回归

Lab1-5：

```text
cargo run -p kernel --features labN --release
```

Lab6-8：

```text
make test-lab6
make test-lab7
make test-lab8
```

### 9.4 静态质量

- `cargo check --workspace`：全 workspace 编译检查
- `cargo clippy --all -- -D warnings`：静态 lint，开启严格告警检查
- `git diff --check`：检查空白错误与冲突标记
- VitePress build 死链检查：同步内容并验证站点构建与内部链接

详细文档：

- `os-lab/docs/day7-demo-runbook.md`：Day7 一键工程验收、演示流程与预演记录
- `os-lab/docs/lab2-m0-acceptance.md`：M0 四契约验收与 Lab2 可信验证基线

---

## 10. 部署、安全与运维

> 初赛基线：本地环境激活脚本。
> 本次新增：Tutor Server、TLS 部署边界、SQLite 备份恢复、数据最小化。

### 10.1 Tutor Server

新增 `os-lab/handbook/tutor-server.mjs`：

- AI 转发
- 终端代跑
- 代码读写
- 脚手架
- 教师接口
- 评分与复核
- 知识库接口
- 备份恢复

### 10.2 安全边界

- 默认只监听 `127.0.0.1`
- 远程课堂放在 TLS 反向代理后
- API Key 不写入仓库、前端包、日志或导出
- CORS 来源显式配置
- 学生运行使用服务端可信 recipe、超时、输出上限
- 跨账号 run 访问返回 404
- 游客模式移除

### 10.3 备份恢复

- 使用 SQLite backup API 在线创建一致性快照
- 每个备份带 SHA-256、migration、表行数 manifest
- 恢复前校验 manifest 与 SHA-256
- 恢复必须离线、显式确认
- 覆盖前保留旧库 `.pre-restore-*.bak`

### 10.4 数据最小化

- 匿名分析默认只输出聚合数据
- 参与者级数据达到最小 cohort 后才输出一次性伪名
- 排除用户名、班级、消息正文、命令、文件路径、原始时间戳

详细文档：

- `os-lab/docs/deployment-and-recovery.md`：部署边界、TLS、备份恢复与数据最小化
- `教师使用指南.md`：教师端部署、开课流程与日常维护

---

## 11. 实验手册与教学材料

> 初赛基线：Lab1-5 手册 + 独立 exercise 目录 + 初赛技术文档。
> 本次新增：Lab6-8 手册、Lab1-8 结构统一、二期文档、教师指南、学习材料。

### 11.1 Lab1-8 手册统一

新增：

- `os-lab/labs/lab6-disk-fs.md`：磁盘文件系统手册，含 VirtIO、easy-fs、硬链接与 fstat
- `os-lab/labs/lab7-ipc-signal.md`：IPC 与信号手册，含统一 fd、dup、信号投递与屏蔽
- `os-lab/labs/lab8-thread-sync.md`：线程与同步手册，含线程、阻塞同步与死锁检测

重构：

- Lab1-5 手册同步统一结构
- 思考题统一放在正文【任务二】
- 删除独立 `labs/exercises/` 目录
- 答案统一在 `labs/answers/`
- 每个 Lab 配 OSTEP 中译章节链接
- 新增 `labs/想法.md`

统一结构：

```text
零、开始之前
一、问题场景
二、背景知识
三、实验任务
四、验证
五、AI 提问模板
```

### 11.2 新增项目文档

- `docs/lab6-8.md`：Lab6-8 二期总览、依赖、分工、syscall 接口与验收
- `os-lab/docs/agent-system-technical.md`：AI 导师、AI 评价、知识库 RAG 技术契约
- `os-lab/docs/ai-tutor-stage-guide.md`：AI 导师意图、提示等级、Prompt 组合与迁移兼容
- `os-lab/docs/lab2-m0-acceptance.md`：Lab2 纵向闭环验收、契约冻结与演示脚本
- `os-lab/docs/day7-demo-runbook.md`：Day7 全链路演示、回归与预演记录
- `os-lab/docs/deployment-and-recovery.md`：部署边界、安全、SQLite 备份与恢复
- `os-lab/docs/member-c-c0-c7-guide.md`：成员 C 在 AI 导师方向的 C0-C7 完成情况与接口
- `教师使用指南.md`：教师部署、开课、任务分配、AI 配置、进度与报告查看
- `Lab手册复核指南.md`：Lab 手册唯一权威源、结构约束、联动位置与验收清单
- `os-lab/handbook/docs/workbench-ui.md`：学习工作台 UI 组件与交互说明
- `os-lab/handbook/docs/day1-workbench-audit.md`：工作台首日审计与缺口记录

### 11.3 新增学习材料

- `RISC-V-Reader-Chinese-v2p1.pdf`：RISC-V ISA 与特权架构中文参考书
- `操作系统导论-OSTEP (王海鹏 译) .pdf`：课程主教材，覆盖虚拟化、并发与持久化
- `8.5.docx`：AI 导师补全、RAG 知识库、AI 评分与 Harness 设计备忘
- `一些小巧思.docx`：AI 双 Agent 拆分理由与 Harness 分层设计笔记

### 11.4 新增手册页面

- `guide/start.md`：入门指南，介绍 os-lab、Lab1-8、OSTEP 映射、学习方式与最短路径
- `guide/beginner.md`：新手操作指南，说明工作台布局、一次 Lab 的完整流程与常见排查
- `guide/ai-tutor.md`：引导式学习入口，选择已开放 Lab 并进入工作台
- `guide/teacher-report.md`：学习评价与教师报告说明页面
- `teacher-review.md`：教师验收学生实验报告与反馈页面
- `teacher/knowledge.md`：教师知识库工作台页面
- `materials.md`：学习材料架，展示 OSTEP 与教师上传材料

---

## 12. 试用验证与后续

> 初赛基线：学习效率评估基于设计推断。
> 本次新增：M5 试用方案、5 人可用性脚本、C0-C7 工作、真实数据校准准备。

### 12.1 M5 试用方案

新增：

- `os-lab/learning/trial-protocol-m5.md`：M5 试用协议，包含目标问题、测量指标、伦理与执行清单
- `os-lab/learning/usability-script-5person.md`：5 人可用性测试脚本，包含任务、台词、记录表与局限

目标问题：

- 工作台 + 证据导师是否降低“只追退出码”的错误完成
- Trace 是否提高“为什么 A 到 B”的解释正确率
- 提示阶梯是否降低直接要答案，同时不显著增加挫败

测量指标：

- 首次 verified 时间
- 提示依赖
- 直接要答案率
- 迁移题
- 延迟测
- 主观负荷

状态：

- 协议与测量设计已完成
- 20 条人工标注轨迹已完成校准
- 真人招募与执行待线下完成

### 12.2 C0-C7 工作

- C0：冻结基线与契约
- C1：可信 Trace 证据链
- C2：AI 导师离线回归 Harness
- C3：服务端导师意图策略与证据门控
- C4：证据行为评价 v3 与掌握画像
- C5：教师复核队列与审计
- C6：Lab Factory 校验、试运行与发布
- C7：匿名试用分析、校准与备份恢复

### 12.3 后续待完成

- 真实课堂试用未执行
- AI 导师真实模型 A/B 仅一轮
- 教师复核队列、掌握画像、Lab Factory、分析与运维前端待完善
- Docker/一体化部署未落地
- 抢占调度、COW、网络、mmap 文件映射等扩展 Lab 未实现

详细文档：

- `os-lab/learning/trial-protocol-m5.md`：M5 试用方案全文
- `os-lab/learning/usability-script-5person.md`：5 人可用性测试执行脚本
- `os-lab/docs/member-c-c0-c7-guide.md`：成员 C 的 C0-C7 工作完成情况与接口说明

---

## 13. 三方对比与定位更新

> 初赛基线：Lab1-5 三方对比。
> 本次新增：Lab1-8 对比数据、8 组件 crate、40+ 单测、平台与 AI 能力定位。

### 13.1 对比数据更新

更新后的自研环境数据：

- 8 个 Lab
- 约 8353 行代码
- 70 个源码文件
- 11 个 workspace crate
- 8 个组件 crate
- 2 层依赖
- 40+ host 单测

对比对象：

- 参考环境 tg-rcore-tutorial：约 36455 行、548 文件、29 crate
- 本校 xv6-riscv：内核约 6000-8000 行 C、11 个 lab、无 crate

### 13.2 定位更新

初赛定位：

> 低门槛入门路径，后续接 xv6 / rCore 深化。

本次定位：

> 不只是入门内核，而是一套可进课堂的教学闭环：
> 8 Lab 内核 + 学习工作台 + 教师端 + AI 导师 + AI 评价 + 可信证据链。

核心差异点保持不变：

- 单内核 feature gate 渐进式
- 8 组件 crate / 2 层依赖
- 问题驱动文档
- Rust 内存安全
- AI 协作模板升级为平台内 AI 双 Agent

详细文档：

- `os-lab/docs/comparison.md`：三方对比分析与学习效率评估
- `os-lab/docs/comparison-data.md`：三方对比原始数据与采集方法

---

## 14. 最近同步记录

- 2026-08-10：Lab8 fill/debug 动手点从 `user/src/bin/lab8_integration_test.rs` 迁至 `kernel/src/sync_syscall.rs`。
- 2026-08-10：修复 GitHub CI 的 Lab7 Lab Factory source assertion，`npm test` 从 94/95 更新为 97/97。
- 2026-08-11：本文档合并自 `new-docs/` 分方向文档。
