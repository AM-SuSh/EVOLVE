# 教师使用指南（本地文档，不进学生手册站点）

> 面向教师/助教。学生看到的使用说明在手册站点「引导式学习」页；本文含任务分配、口令等教师侧信息。

## 一、总览：账号制 + 教师控制台

- **站点入口即登录**：打开学习平台首先是登录页（学生注册需填**班级**；账号存 `os-lab/learning/os-lab.db`）；系统代码、进度、任务变体、提交的报告全部绑定账号，工作区为 `student-labs/<用户名>/`；「游客浏览」仅限只读。
- **教师管理端（与学生页完全不同）**：admin 登录后自动跳转 **`/teacher`**（其他页面右下角常驻「教师端」按钮）。八块功能：发布对象三级选择（全局/班级/学生）→ 作业公告、开放进度（下发阶段代码）、任务分配（不同类型作业）均按对象生效（学生覆盖>班级覆盖>全局）；**实验指导在线编辑**（改 Markdown 保存自动同步站点，与时俱进补充知识）；AI 统一配置；各班级学生完成情况（班级筛选+每人生效开放进度）；学生报告在线查看。
  1. **开放进度**：选择开放到第几个 Lab——学生只能升级到已开放层级（未开放的领不到代码）；
  2. **任务分配**：给每个 Lab 下发任务变体（补全 `fill` / 排错 `debug`，或在任务类型下拉框选 `random` 让每位学生随机拿到一种具体变体；随机结果在「个别调整」选择学生后可见）；
  3. **AI 导师统一配置**：填接口地址/模型/Key 后全班默认使用；「允许学生自配」勾选与否决定学生自己的模型设置是否生效（不勾 = 全班强制统一）；
  4. **学生进度**：全班列表——每人做到哪个 Lab、拿的什么任务变体、自建了哪些程序。
- **教师=预置管理员**：首次启动自动创建 `admin / admin123`（**首登后立即在账号弹窗改密码**）。admin 登录 `/teacher` 即用全部管理功能（学生进度/报告均带班级列并可按班筛选）；学生账号访问被拒。教学配置存 `os-lab/scaffold/teacher.json`（不进 git）。

## 二、部署（每台机器/机房服务器一次）

```powershell
# 1. 环境（Rust/QEMU）见手册「环境安装」页
# 2. 启动导师服务（保持运行；AI 转发、终端代跑、代码读写、脚手架、教师接口都靠它）
cd os-lab/handbook
npm ci
npm run tutor        # 含账号数据库；首次启动预置 admin/admin123

# 3. 另开终端启动站点
npm run dev          # 本机预览（默认 5173 端口）
npm run build        # 或构建后静态部署 .vitepress/dist
```

## 三、开课流程（示例）

1. 打开 `/teacher`，用 admin 登录（首登改密码）；
2. 「作业公告」发布本周任务（显示在每个学生工作台横幅）；
3. 「教学进度」设开放到 `lab1`（或本周进度）——这就是「下发不同阶段的代码」；
4. 「任务分配」给 lab2 任务类型选 `random`（或统一选 `fill`/`debug`）后点「下发」；
5. （可选）「AI 统一配置」填班级共用的 API，按需取消「允许学生自配」；
6. 学生在站点入口注册（填班级）→ 初始化系统 → 学习并在报告面板「提交给老师」；
7. 教师端「学生进度」看全班推进，「学生提交的报告」直接在线查看（重复提交覆盖旧版）；每周把开放进度往后拨一格。

命令行备用（与控制台等价）：`node scripts/scaffold.mjs open lab3 | assign lab2 debug | list | status <学号>`。

## 四、制作新的任务变体（Lab3-8 待补，两步法）

1. 在 `os-lab/scripts/scaffold.mjs` 的 `EXERCISES` 表登记：

   ```js
   lab3: {
     default: 'fill',
     variants: {
       fill:  { label: '补全：实现页表映射', files: ['kernel/src/mm.rs'] },
       debug: { label: '排错：映射权限有一个 bug', files: ['kernel/src/mm.rs'] },
     },
   },
   ```

2. 把改好的文件放到 `os-lab/scaffold/exercises/lab3/fill/kernel/src/mm.rs`（及 `debug/...`）。

要求：挖空用 `todo!("提示")` 保证能编译；**debug 变体的 bug 必须在该 Lab 执行流中真实显形**（先自己跑一遍确认症状——Lab2 首版就因埋错位置不显形返工过）；文件头写清任务说明、现象与排查路径（参照 Lab2 两个变体）。登记后控制台的「任务分配」会自动出现该 Lab。

## 五、查看学习过程与评分

- 评分报告：站点 `/guide/teacher-report`（算法单源 `os-lab/learning/rubric.mjs`）；
- 学习事件：服务端按账号落盘到 `os-lab/learning/student-data/<userId>/events/<sessionId>.jsonl`，浏览器只保留离线副本；
- 学生学习数据统一目录：`student-data/<userId>/reports/<labId>/` 存报告草稿与正式提交，`conversations/` 存 AI 会话，`runs/<labId>/<runId>/` 存运行输出与 Trace；SQLite `os-lab.db` 负责索引、权限、状态、评分和教师查询；
- 学生解锁规则（答疑常用）：上一层**一次成功运行**（终端退出码 0 自动记录，需导师服务在跑）**+ 一次带反思的报告保存**即解锁下一层页面；**系统代码升级**另受教师「开放进度」限制——两条线独立。

## 六、维护教学内容

- 手册正文/答案：只改 `os-lab/labs/`（见《Lab手册复核指南.md》）；
- 导师人设与护栏：`os-lab/tutor/prompts/`；
- 参考实现改动后：同步检查 `scripts/scaffold.mjs` 文件清单与变体文件。

## 七、常见问题

- **学生打不开 Lab N**：按第五节两条件检查；系统升级被拒看是否「未开放」。
- **学生模型连不上/中途断**：设置弹窗显示具体原因；单条消息失败不影响连接，重发即可；若教师已强制统一，学生弹窗会提示「由老师统一配置」。
- **重置某个学生**：代码工作区删 `student-labs/<学号>/`；若需同时清理学习数据，按账号 ID 删除 `learning/student-data/<userId>/` 并保留 SQLite 备份；换身份 = 学生点顶栏名字重填。
- **多台机器教学**：控制台配置在每台跑 tutor-server 的机器上各自生效；机房建议共用一台服务器跑 tutor-server + 站点，学生浏览器访问同一地址（`OS_LAB_TUTOR_ORIGIN` 加上站点来源，前端 `.env.local` 里把 `VITE_OS_LAB_TUTOR_ENDPOINT` 指到服务器）。
